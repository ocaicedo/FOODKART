<h1>Orden</h1>
  
<table>
  <tr>
    <th>Cliente</th>
    <th>Número</th>
    <th>Fecha</th>
    <th>Descuento</th>
    <th>Total</th>
  </tr>
 
  <% @orders.each do |order| %>
    <tr>
      <td><%= order.customer_id %></td>
      <td><%= order.id %></td>
      <td><%= order.date%></td>
      <td><%= link_to 'Detalles', order_path(order) %></td>
      <td><%= link_to 'Modificar Orden', edit_order_path(order) %></td>
      
    </tr>
  <% end %>
  <%= link_to 'Crear Nueva  Orden', new_order_path %>
</table>